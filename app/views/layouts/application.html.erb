<!DOCTYPE html>
<html>
  <head>
    <title>Final Project</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <style>
      body { background: #f5f6fa; font-family: var(--bs-body-font-family, system-ui); color: #212529; }
      .navbar-brand { font-weight: bold; letter-spacing: 1px; }
      .main-content { margin-top: 80px; }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top shadow">
      <div class="container">
        <a class="navbar-brand" href="<%= root_path %>">Final Project</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <% if user_signed_in? %>
              <li class="nav-item"><%= link_to 'Feed', root_path, class: 'nav-link' %></li>
              <li class="nav-item"><%= link_to 'Usuários', users_path, class: 'nav-link' %></li>
              <li class="nav-item"><%= link_to 'Meu Perfil', user_path(current_user), class: 'nav-link' %></li>
              <li class="nav-item"><%= link_to 'Novo Post', new_post_path, class: 'nav-link' %></li>
            <% else %>
              <li class="nav-item"><%= link_to 'Login', new_user_session_path, class: 'nav-link' %></li>
              <li class="nav-item"><%= link_to 'Cadastrar', new_user_registration_path, class: 'nav-link' %></li>
            <% end %>
          </ul>
          <% if user_signed_in? %>
            <%= button_to 'Sair', destroy_user_session_path, method: :delete, form: { class: 'd-inline' }, class: 'btn btn-outline-light ms-lg-3' %>
          <% end %>
        </div>
      </div>
    </nav>
    <div class="container main-content pb-5">
      <%= yield %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
  </body>
</html>
