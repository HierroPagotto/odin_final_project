<h1 class="mb-4 text-success text-center">Novo Post</h1>
<div class="row justify-content-center">
  <div class="col-12 col-md-8 col-lg-6">
    <div class="card shadow-sm p-4">
      <%= form_with(model: @post, local: true) do |form| %>
        <div class="mb-3">
          <%= form.label :content, 'Conteúdo', class: 'form-label' %>
          <%= form.text_area :content, rows: 4, class: 'form-control' %>
        </div>
        <div class="d-grid">
          <%= form.submit 'Criar Post', class: 'btn btn-success' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
